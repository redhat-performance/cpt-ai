$ curl -s -X POST http://localhost:8000/ask -H "Content-Type: application/json" -d '{"run1": {"cloud": "azure", "os_vendor": "rhel", "os_version": "9.6", "instance": "Standard_D96ds_v6", "benchmark": "passmark"}, "run2": {"cloud": "azure", "os_vendor": "rhel", "os_version": "9.6", "instance": "Standard_D64ds_v6", "benchmark": "passmark"}, "detail_level": "medium", "question": "Show me the math behind the geomean calculation"}' | jq

{
  "answer": "The benchmark results show an overall regression of -16.37%. This was calculated using a geometric mean of the ratios between the matched metrics in Run 2 and Run 1. Here's the breakdown:\n\n1.  **Identify Matched Metrics:** The analysis only considers metrics that are present in both runs. In this case, 26 metrics were matched after applying skip rules. For example, the script only considers the `*_mean` metric and skips the `*_min`, `*_max`, and `*_stddev` variants.\n\n2.  **Calculate Per-Metric Ratios:** For each matched metric, a ratio is calculated by dividing the value from Run 2 by the value from Run 1. For example:\n\n    *   `CPU_COMPRESSION_mean`: Run1=1345954.176809, Run2=910202.736342, ratio = 910202.736342 / 1345954.176809 = 0.676251\n    *   `CPU_ENCRYPTION_mean`: Run1=84087.953488, Run2=56252.544278, ratio = 56252.544278 / 84087.953488 = 0.668973\n    *   `CPU_PHYSICS_mean`: Run1=8660.239178, Run2=9900.907204, ratio = 9900.907204 / 8660.239178 = 1.143260\n\n    The per-metric ratios are listed in the report.\n\n3.  **Calculate the Geometric Mean:** The geometric mean is calculated as follows:\n\n    *   Take the natural logarithm of each ratio.\n    *   Sum the logarithms.\n    *   Divide the sum by the number of matched metrics (N=26).\n    *   Take the exponential of the result.\n\n    Formula: geomean = exp(sum(log(ratio\\_i)) / N)\n\n    In this specific case:\n\n    *   `geomean` = exp((log(0.676251) + log(0.668973) + log(0.667936) + log(0.667721) + log(0.719675) + log(1.143260) + log(0.817025) + log(1.000628) + log(0.723704) + log(0.755985) + log(0.684350) + log(0.700273) + log(0.673598) + log(0.705596) + log(1.281627) + log(0.661507) + log(0.570816) + log(1.436140) + log(1.012646) + log(1.546460) + log(1.429741) + log(0.802040) + log(1.517314) + log(0.670765) + log(0.667722) + log(0.667494)) / 26)\n    *   `geomean` = exp((-0.3911 + -0.4014 + -0.4029 + -0.4032 + -0.3287 + 0.1337 + -0.2020 + 0.0006 + -0.3232 + -0.2797 + -0.3787 + -0.3564 + -0.3954 + -0.3487 + 0.2479 + -0.4125 + -0.5613 + 0.3620 + 0.0126 + 0.4358 + 0.3577 + -0.2207 + 0.4170 + -0.3996 + -0.4032 + -0.4035) / 26)\n    *   `geomean` = exp(-4.6908 / 26)\n    *   `geomean` = exp(-0.1804)\n    *   `geomean` = 0.836280\n\n4.  **Calculate the Delta Percentage:** The delta percentage represents the overall change between the two runs. It's calculated as:\n\n    *   delta% = (geomean - 1) \\* 100\n    *   delta% = (0.836280 - 1) \\* 100 = -16.37%\n\nIn summary, the geometric mean provides a single number that represents the aggregate change across all the matched metrics, accounting for both improvements and regressions. In this case, the negative delta indicates an overall regression in performance.\n"
}
