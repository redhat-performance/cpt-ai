# Examples

Captured outputs from API and CLI sessions for reference.

## Folders

| Folder | Contents |
|--------|----------|
| `api/` | Captured API request/response samples (filters, compare, ask) |
| `cli/` | Recorded CLI session outputs (passmark, streams, coremark-pro, pyperf) |

These examples show real outputs and can be used to verify the system is working correctly.
